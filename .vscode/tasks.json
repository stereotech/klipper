{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Copy config",
            "command": "cp",
            "args": [
                "-r",
                "${workspaceFolder}/stereotech_config/",
                "${workspaceFolder}/test/config"
            ]
        },
        {
            "label": "Copy",
            "type": "shell",
            "command": "cp",
            "args": [
                "${workspaceFolder}/test/config/H*",
                "${workspaceFolder}/test/"
            ]
        }
    ]
}